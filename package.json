{
  "name": "mnemonic",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A personal memory system using FSRS spaced repetition.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch --serve",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "esbuild": "^0.27.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0"
  }
}
